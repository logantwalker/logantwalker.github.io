import React from 'react';
import Navbar from '../Navbar/Navbar';
import 'materialize-css'

const Layout = (props) => {
    return (
        <React.Fragment>
            <Navbar/>
        </React.Fragment>
    )
}

export default Layout;